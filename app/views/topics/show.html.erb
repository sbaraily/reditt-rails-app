<h1><%= @sub.name %> - <%= @topic.name %></h1>

<%= @topic.body %>

<%= link_to 'edit topic', edit_sun_topic_path([@sub, @topic]) %>
<%= link_to 'Delete Topic', [@sub, @topic], method: :delete %>
<%= link_to 'back to topic', sun_topics_path(@sub) %>
